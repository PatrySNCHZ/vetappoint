package es.vetappoint.service.simpl;
/*
import es.vetappoint.dao.UsuarioDao;
import es.vetappoint.dao.service.EncryptService;
import es.vetappoint.entities.Usuario;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.security.crypto.bcrypt.BCrypt;
import org.springframework.stereotype.Service;

@Service
public class EncryptServiceImpl implements EncryptService {

    @Autowired
    private UsuarioDao usuarioDao;

    @Override
    public String encryptClave (String clave) {
        return BCrypt.hashpw(clave, BCrypt.gensalt());
    }

    @Override
    public boolean verifyClave(String originalClave, String hashClave) {
        return false;
    }

}
*/